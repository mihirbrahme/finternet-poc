# Finternet Explorer

Light operational explorer scaffold for Packet 5 tokenised recovery assets.

Open `index.html` in a browser to inspect the static Packet 5 read model shape. Later packets can replace the sample payload with the blockchain-indexer read model without changing the visible fields.
