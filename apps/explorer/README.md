# Finternet Explorer

Light operational explorer scaffold for Packet 8 tokenised recovery assets, attestations and claims.

Open `index.html` in a browser to inspect the static read model shape. It shows receipt attestations and an `OBP_READY_RECOVERY_CLAIM` with `isOfficialCredit=false`, evidence references and boundary copy that it is not official credit issuance.
